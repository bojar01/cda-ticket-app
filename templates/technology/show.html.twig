{% extends 'baseDashboard.html.twig' %}

{% block title %}Technology{% endblock %}

{% block body %}
    <h1>Technology</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ technology.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ technology.name }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn perso-button-main" href="{{ path('app_technology_index') }}">Retour à la liste</a>

    <a class="btn perso-button-main my-4" href="{{ path('app_technology_edit', {'id': technology.id}) }}">Modifier</a>

    {{ include('technology/_delete_form.html.twig') }}
{% endblock %}
