<form method="post" action="{{ path('app_ticket_unsetangel', {'id': ticket.id}) }}" onsubmit="return confirm('Are you sure you want to help this person?');">
    <input type="hidden" name="_token" value="{{ csrf_token('unsetangel' ~ ticket.id) }}">
    <button class="btn btn-warning">Retourner en file d'attente</button>
</form>
