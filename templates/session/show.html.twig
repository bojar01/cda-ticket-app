{% extends 'baseDashboard.html.twig' %}

{% block title %}Session{% endblock %}

{% block body %}
    <h1>Session</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ session.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ session.name }}</td>
            </tr>
            <tr>
                <th>DiscordChannelLink</th>
                <td>{{ session.discordChannelLink }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn perso-button-main" href="{{ path('app_session_index') }}">Retour à la liste</a>

    <a class="btn perso-button-main my-4" href="{{ path('app_session_edit', {'id': session.id}) }}">Modifier</a>

    {{ include('session/_delete_form.html.twig') }}
{% endblock %}
