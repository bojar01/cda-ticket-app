<form class="text-center" action="{{ path('app_search') }}" method="get">
        <div class="d-flex justify-content-center">
            <input class="w-50" type="text" name="r" placeholder="Rechercher...">
            <button type="submit" class="btn perso-button-main w-25">Rechercher</button>
        </div>
        {# <div class="d-flex flex-column text-start w-75 m-auto">
            <label class="my-2">Status</label>
            <select class="form-select" name="status">
                <option value="all">Tous</option>
                <option value="resolved">Résolu</option>
                <option value="pending">En attente</option>
            </select>
        </div> #}
</form>